[project]
name = "as-lobster-baseline-bundle"
version = "0.1.0"
description = "Portable Avellaneda-Stoikov LOBSTER baseline runner"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.26",
    "pandas>=2.2",
    "matplotlib>=3.8",
]

[tool.uv]
package = false

